#include "main.h"

/**
  * puts_half - prints the back half of string
  *
  * Return: void (success)
  */

void puts_half(char *str)
{
	int v = 0;

	while (str[v] != '\n' && str[v] != '\0')
	{
		v++;
	}
	for (



	}
	_putchar('\n');
}
