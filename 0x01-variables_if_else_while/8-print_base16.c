#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
	int g;

	for (g = 48; g <= 57; g++)
		putchar(n);
	for (g = 97; g <= 102; g++)
		putchar(g);
	putchar(10);
	return (0);
}
